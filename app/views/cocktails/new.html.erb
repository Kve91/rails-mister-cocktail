<h1>New Cocktail</h1>

<%= render 'form', cocktail: @cocktail %>

<!-- app/views/articles/_form.html.erb -->
<h2>Looks cool but doesnt actually work!</h2>
<%= simple_form_for(@cocktail) do |f| %>
  <!-- [...] -->
  <%= f.input :photo %>
  <%= f.input :photo_cache, as: :hidden %>
  <!-- [...] -->
<% end %>

